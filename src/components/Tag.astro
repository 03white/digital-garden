---
import { withBase } from '../lib/site';

interface Props {
  name: string;
  count?: number;
}

const { name, count } = Astro.props;
---

<a class="tag" href={withBase(`/tags/${encodeURIComponent(name)}/`)}>
  #{name}{count !== undefined ? ` · ${count}` : ''}
</a>
