---
import { SITE, withBase } from '../lib/site';
---

<header class="site-header">
  <a class="site-title" href={withBase('/')}>{SITE.title}</a>
  <nav class="site-nav" aria-label="主导航">
    {SITE.navLinks.map((link) => <a href={withBase(link.href)}>{link.label}</a>)}
  </nav>
</header>
