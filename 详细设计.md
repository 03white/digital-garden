# Digital Garden 静态博客详细设计

## 1. 技术选型

### 1.1 静态站点框架：Astro

本项目建议选择 Astro 作为静态站点框架。

选择原因：

- 原生适合内容型网站、博客和文档站。
- Markdown / MDX 支持成熟。
- 默认以静态 HTML 输出为主，性能好。
- 支持组件化开发，后续可引入 React、Vue、Svelte 等 UI 组件。
- 支持 Content Collections，便于约束文章和笔记元数据。
- GitHub Pages 部署方案清晰。

备选方案对比：

| 方案 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- |
| Astro | 内容站友好、性能好、扩展强 | 需要自行设计部分数字花园能力 | 推荐 |
| Quartz | Obsidian 数字花园能力强 | 定制前端和结构时自由度较低 | 适合强 Obsidian 工作流 |
| VitePress | 文档体验好、配置简单 | 博客和数字花园表达稍弱 | 适合技术文档站 |
| Jekyll | GitHub Pages 原生支持 | 开发现代化程度一般 | 不作为首选 |

### 1.2 内容格式：Markdown + MDX

- 默认使用 Markdown 编写文章和笔记。
- 后续如需在内容中嵌入交互组件，可启用 MDX。
- 文章元数据通过 frontmatter 管理。

### 1.3 样式方案：原生 CSS + CSS Variables

第一阶段建议使用原生 CSS，不引入复杂 UI 框架。

原因：

- 静态博客样式需求可控。
- 减少依赖和构建复杂度。
- CSS 变量便于后续支持主题切换。

后续如果需要更快构建复杂 UI，可再评估 Tailwind CSS。

### 1.4 部署平台：GitHub Pages

- 代码托管在 GitHub 仓库。
- 使用 GitHub Actions 构建 Astro 项目。
- 使用 GitHub Pages 托管构建产物。

### 1.5 包管理器

建议使用 `pnpm` 或 `npm`。

若希望环境最简单，第一阶段可使用 `npm`；若更关注安装速度和依赖管理，可使用 `pnpm`。

建议默认：`pnpm`。

## 2. 项目目录设计

建议目录结构如下：

```text
digital-garden/
├─ .github/
│  └─ workflows/
│     └─ deploy.yml
├─ public/
│  ├─ favicon.svg
│  └─ images/
├─ src/
│  ├─ components/
│  │  ├─ Header.astro
│  │  ├─ Footer.astro
│  │  ├─ PostCard.astro
│  │  └─ Tag.astro
│  ├─ content/
│  │  ├─ config.ts
│  │  ├─ posts/
│  │  │  └─ hello-world.md
│  │  └─ notes/
│  │     └─ first-note.md
│  ├─ layouts/
│  │  ├─ BaseLayout.astro
│  │  └─ ContentLayout.astro
│  ├─ lib/
│  │  ├─ content.ts
│  │  └─ site.ts
│  ├─ pages/
│  │  ├─ index.astro
│  │  ├─ about.astro
│  │  ├─ posts/
│  │  │  ├─ index.astro
│  │  │  └─ [slug].astro
│  │  ├─ notes/
│  │  │  ├─ index.astro
│  │  │  └─ [slug].astro
│  │  └─ tags/
│  │     ├─ index.astro
│  │     └─ [tag].astro
│  └─ styles/
│     └─ global.css
├─ astro.config.mjs
├─ package.json
├─ pnpm-lock.yaml
├─ README.md
├─ 一般需求分析.md
└─ 详细设计.md
```

## 3. 内容模型设计

### 3.1 Posts 集合

`posts` 用于正式文章。

字段设计：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `title` | string | 是 | 文章标题 |
| `description` | string | 是 | 文章摘要 |
| `pubDate` | date | 是 | 发布时间 |
| `updatedDate` | date | 否 | 更新时间 |
| `tags` | string[] | 否 | 标签列表 |
| `draft` | boolean | 否 | 是否草稿，默认 false |

示例：

```md
---
title: 第一篇文章
description: 这是 digital garden 的第一篇文章。
pubDate: 2026-08-15
tags: [blog, astro]
draft: false
---

这里是文章正文。
```

### 3.2 Notes 集合

`notes` 用于成长型笔记。

字段设计：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `title` | string | 是 | 笔记标题 |
| `description` | string | 否 | 笔记摘要 |
| `createdDate` | date | 是 | 创建时间 |
| `updatedDate` | date | 否 | 更新时间 |
| `tags` | string[] | 否 | 标签列表 |
| `status` | enum | 否 | `seedling`、`growing`、`evergreen` |
| `draft` | boolean | 否 | 是否草稿，默认 false |

笔记状态说明：

- `seedling`：刚记录的想法或片段。
- `growing`：正在整理和扩展的内容。
- `evergreen`：相对成熟、长期维护的笔记。

示例：

```md
---
title: 数字花园是什么
description: 关于 digital garden 的概念笔记。
createdDate: 2026-08-15
updatedDate: 2026-08-15
tags: [digital-garden, writing]
status: seedling
draft: false
---

数字花园是一种持续生长的知识组织方式。
```

## 4. 页面设计

### 4.1 首页 `/`

首页模块：

- Hero 区域：站点名称、简介、主要入口。
- 最新文章：展示最近 3 到 5 篇文章。
- 最近笔记：展示最近更新的 3 到 5 条笔记。
- 标签入口：展示常用标签。

### 4.2 关于页 `/about/`

内容建议：

- 作者简介。
- 站点定位。
- 内容主题。
- 联系方式或 GitHub 链接。

### 4.3 文章列表 `/posts/`

行为：

- 读取 `posts` 集合。
- 过滤 `draft: true` 内容。
- 按 `pubDate` 倒序排序。
- 使用 `PostCard` 组件展示。

### 4.4 文章详情 `/posts/[slug]/`

行为：

- 根据 slug 生成静态路径。
- 渲染 Markdown 正文。
- 展示文章元信息。
- 展示标签链接。

### 4.5 笔记列表 `/notes/`

行为：

- 读取 `notes` 集合。
- 过滤草稿内容。
- 按 `updatedDate` 或 `createdDate` 倒序排序。
- 展示笔记状态。

### 4.6 笔记详情 `/notes/[slug]/`

行为：

- 根据 slug 生成静态路径。
- 渲染 Markdown 正文。
- 展示创建时间、更新时间、状态和标签。

### 4.7 标签页 `/tags/` 与 `/tags/[tag]/`

标签页行为：

- 汇总所有文章和笔记中的标签。
- 展示标签名称和内容数量。

单标签页行为：

- 展示指定标签下的文章和笔记。
- 内容可按更新时间倒序排列。

## 5. 组件设计

### 5.1 `BaseLayout.astro`

职责：

- 输出 HTML 基础结构。
- 设置页面标题、描述和基础 SEO 信息。
- 引入全局样式。
- 包含 Header 和 Footer。

### 5.2 `ContentLayout.astro`

职责：

- 承载文章和笔记详情页布局。
- 统一正文宽度、标题、元信息和标签展示。

### 5.3 `Header.astro`

职责：

- 展示站点名称。
- 提供导航链接：首页、文章、笔记、标签、关于。

### 5.4 `Footer.astro`

职责：

- 展示版权信息。
- 展示 GitHub 链接或构建信息。

### 5.5 `PostCard.astro`

职责：

- 展示文章或笔记卡片。
- 包含标题、摘要、日期、标签。

### 5.6 `Tag.astro`

职责：

- 统一标签视觉样式。
- 可链接到对应标签页。

## 6. 数据处理设计

### 6.1 内容读取

使用 Astro 的 `getCollection` 读取内容集合。

需要封装的工具函数：

- `getPublishedPosts()`：获取非草稿文章。
- `getPublishedNotes()`：获取非草稿笔记。
- `getAllTags()`：汇总所有标签。
- `getEntriesByTag(tag)`：获取指定标签下的内容。

### 6.2 排序规则

- 文章按 `pubDate` 倒序。
- 笔记按 `updatedDate || createdDate` 倒序。
- 标签按内容数量或字母顺序排序。

### 6.3 草稿规则

- 本地开发可以看到草稿，作为后续增强。
- 生产构建默认过滤 `draft: true`。

第一阶段可统一过滤草稿，避免误发布。

## 7. 路由设计

| 路由 | 页面 | 说明 |
| --- | --- | --- |
| `/` | 首页 | 站点入口 |
| `/about/` | 关于页 | 作者和站点说明 |
| `/posts/` | 文章列表 | 全部正式文章 |
| `/posts/[slug]/` | 文章详情 | 单篇文章 |
| `/notes/` | 笔记列表 | 全部笔记 |
| `/notes/[slug]/` | 笔记详情 | 单篇笔记 |
| `/tags/` | 标签列表 | 全部标签 |
| `/tags/[tag]/` | 标签聚合 | 指定标签内容 |

## 8. 样式设计

### 8.1 视觉原则

- 简洁、清爽、强调阅读体验。
- 保持较大的正文行高和合理宽度。
- 链接、标签、代码块样式清晰可辨。
- 移动端优先，桌面端增强。

### 8.2 CSS 变量

建议定义：

```css
:root {
  --color-bg: #ffffff;
  --color-text: #1f2937;
  --color-muted: #6b7280;
  --color-link: #2563eb;
  --color-border: #e5e7eb;
  --font-sans: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-mono: "SFMono-Regular", Consolas, "Liberation Mono", monospace;
}
```

### 8.3 响应式设计

- 主内容宽度建议控制在 `720px` 到 `860px`。
- 小屏下导航可折行或简化。
- 卡片列表在桌面端可适当增加间距，在移动端保持单列。

## 9. SEO 设计

第一阶段支持：

- 每页独立 `title`。
- 每页独立 `description`。
- 基础 Open Graph 标签。
- 语义化结构：`header`、`main`、`footer`、`article`。

后续增强：

- `@astrojs/sitemap` 生成 sitemap。
- RSS feed。
- 社交分享图。

## 10. GitHub Pages 部署设计

### 10.1 Astro 配置

如果部署为 GitHub Pages 项目页，URL 通常为：

```text
https://<username>.github.io/digital-garden/
```

则 Astro 配置中需要关注：

```js
export default defineConfig({
  site: 'https://<username>.github.io',
  base: '/digital-garden',
});
```

如果部署到用户主页仓库，例如 `<username>.github.io`，则通常不需要 `base`。

### 10.2 GitHub Actions 流程

部署流程：

1. 检出代码。
2. 安装 Node.js。
3. 安装依赖。
4. 构建 Astro 项目。
5. 上传 Pages artifact。
6. 发布到 GitHub Pages。

权限建议：

```yaml
permissions:
  contents: read
  pages: write
  id-token: write
```

## 11. 开发命令设计

建议脚本：

```json
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  }
}
```

常用命令：

- `pnpm dev`：本地开发预览。
- `pnpm build`：生产构建。
- `pnpm preview`：预览构建结果。
- `pnpm check`：类型和内容检查。

## 12. 迭代计划

### 12.1 第一阶段：基础博客骨架

- 初始化 Astro 项目。
- 建立文章和笔记内容集合。
- 实现首页、文章、笔记、标签、关于页。
- 添加基础样式。
- 配置 GitHub Pages 部署。

### 12.2 第二阶段：阅读体验增强

- 添加目录导航。
- 添加代码高亮主题优化。
- 添加 RSS。
- 添加 sitemap。
- 添加深色模式。

### 12.3 第三阶段：数字花园增强

- 支持内部链接约定。
- 支持反向链接。
- 支持相关内容推荐。
- 支持图谱视图。
- 支持全文搜索。

### 12.4 第四阶段：互动与统计

- 添加评论系统。
- 添加访问统计。
- 添加社交分享。
- 添加订阅入口。

## 13. 实施注意事项

- 不要一开始引入过重依赖。
- 优先保证写作、构建、部署闭环跑通。
- 内容 schema 要尽早固定，减少后续迁移成本。
- 样式先保持简洁，后续再逐步视觉升级。
- GitHub Pages 的 `base` 配置应根据实际仓库名确认。

## 14. 后续待确认问题

在进入编码实现前，建议确认：

- GitHub 用户名或组织名是什么。
- 仓库最终是否就叫 `digital-garden`。
- 是否使用 `pnpm` 作为包管理器。
- 是否需要中英文双语内容。
- 是否计划与 Obsidian 工作流联动。
- 是否需要自定义域名。
